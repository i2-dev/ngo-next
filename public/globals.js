// globals.js - 全局 JavaScript 文件
// 這個文件會在所有頁面加載

// 確保在瀏覽器環境中執行
if (typeof window !== 'undefined') {
    // console.log('✅ globals.js 加載完成');
    
    // Header scroll effect is now handled by React component (HeaderScrollEffect.js)
    // to prevent hydration mismatches
}
